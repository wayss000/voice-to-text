{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ],
    "env": {
        "VOLC_APP_ID": "@volc_app_id",
        "VOLC_TOKEN": "@volc_token",
        "VOLC_CLUSTER": "@volc_cluster"
    }
}